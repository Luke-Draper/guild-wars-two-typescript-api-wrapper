import Achievements from './achievements'
/**
 * Achievements test
 */
describe('Achievements test', () => {
	test('Achievements is instantiable', () => {
		expect(new Achievements()).toBeInstanceOf(Achievements)
	})
})
