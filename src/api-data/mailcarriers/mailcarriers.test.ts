import Mailcarriers from './mailcarriers'
/**
 * Mailcarriers test
 */
describe('Mailcarriers test', () => {
	test('Mailcarriers is instantiable', () => {
		expect(new Mailcarriers()).toBeInstanceOf(Mailcarriers)
	})
})
