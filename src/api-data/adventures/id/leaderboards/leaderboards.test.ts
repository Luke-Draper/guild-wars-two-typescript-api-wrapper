import Leaderboards from './leaderboards'
/**
 * Leaderboards test
 */
describe('Leaderboards test', () => {
	test('Leaderboards is instantiable', () => {
		expect(new Leaderboards()).toBeInstanceOf(Leaderboards)
	})
})
