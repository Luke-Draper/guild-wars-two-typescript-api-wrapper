import Adventures from './adventures'
/**
 * Adventures test
 */
describe('Adventures test', () => {
	test('Adventures is instantiable', () => {
		expect(new Adventures()).toBeInstanceOf(Adventures)
	})
})
